# Prediction presentation bindings

Each module coordinates one part of the Prediction workspace. Model discovery,
prediction, persistence, and file access continue to flow through the injected
ViewModel and application ports.
