# Format Converter presentation bindings

The main dialog composes independent input selection, preview, output options,
workflow navigation, and conversion-progress bindings. Conversion and file I/O
remain in the ViewModel/use case/adapters.
