# WAXS presentation bindings

The WAXS page composes focused form, loading, selection, integration/export,
batch, and display-state bindings. Numerical geometry and integration remain in
domain/application services and external I/O stays behind adapters.
