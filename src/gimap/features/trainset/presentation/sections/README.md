# Trainset page sections

These modules build and update one visible Trainset workflow section each. They
own Qt layout/rendering only and communicate through the page's public state and
signals; simulation and persistence remain in application use cases/adapters.
