# Fitting page sections

Each module creates one stable part of the Fitting page while preserving the
existing widget attributes and object names. The top-level `FittingPageView`
only composes these sections and applies translations.
